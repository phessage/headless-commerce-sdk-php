# Architecture

The SDK is framework-neutral and depends only on PHP. Transport is injectable for PSR/framework adapters and deterministic tests. Its public model follows `/v1/headless/products`; additional domains will be added only after their contracts stabilize.
